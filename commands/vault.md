---
description: Creative Agent Vault dashboard — personal memory at a glance
---

Show the **Agent Vault creative dashboard**.

## Steps

1. Ensure vault: `python "$GROK_PLUGIN_ROOT/scripts/ensure_vault.py"` if needed.
2. Run `python "$GROK_PLUGIN_ROOT/scripts/vault_status.py"` and paste/present the framed output as-is (preserve the box art).
3. **Never** open or print secret **values** from `me/.private/`.
4. Optionally ask once: anything to pin as a reminder?
5. Mention `/vaultkeys` for sealed key labels only.

Do not rewrite the dashboard into plain bullets unless the user asks for a compact form.
