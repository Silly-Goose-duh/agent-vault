---
description: List sealed key labels only (never values)
---

1. Run `python "$GROK_PLUGIN_ROOT/scripts/vault_keys.py"`.
2. Show When / Kind / Label / Source only.
3. Never print the Value column.
